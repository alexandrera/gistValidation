package com.restAssured;

import com.aventstack.extentreports.ExtentReports;
import com.aventstack.extentreports.reporter.ExtentSparkReporter;

import java.util.Base64;

public class myClass {

    public static void main (String[]Args) {

        System.out.println("API validation with rest assured");

        //  Uncomment bellow and put your github token in original string
        //  Run this method and get the encoded string in the console
        //  This is just to encrypt

        //  Base64.Encoder encoder = Base64.getEncoder();
        //  String originalString = "Bearer MY_GITHUB_TOKEN";
        //  String encodedString = encoder.encodeToString(originalString.getBytes());
        //  System.out.println(encodedString);

    }

}